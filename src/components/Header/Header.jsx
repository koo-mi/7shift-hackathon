import './Header.scss'

const Header = () => {
    return (
        <header className='header'>
            <nav className='nav'>

            </nav>
        </header>
    );
};

export default Header;